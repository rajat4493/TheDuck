{
  "name": "theduck-intent-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.js",
    "check": "node --check server.mjs && node --check src/intent.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22"
  }
}
