{
  "name": "theduck-intent-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "test": "node --test test/*.test.js",
    "check": "node --check server.mjs && node --check src/intent.js && node --check src/experience.js && node --check src/semantic.js && node --check src/provider.mjs && node --check public/app.js",
    "verify:live": "node --env-file-if-exists=.env.local scripts/verify-live.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
